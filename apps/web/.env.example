# Database
DATABASE_URL="file:./dev.db"

# Auth0 (required for authentication)
AUTH0_SECRET=""          # Run: openssl rand -hex 32
AUTH0_BASE_URL="http://localhost:3000"
AUTH0_ISSUER_BASE_URL="" # https://YOUR_TENANT.auth0.com
AUTH0_CLIENT_ID=""
AUTH0_CLIENT_SECRET=""

# Encryption (required for travel documents)
ENCRYPTION_KEY=""        # Run: openssl rand -hex 32

# Inbound email webhook (required for /api/email/inbound)
WEBHOOK_EMAIL_SECRET=""           # Run: openssl rand -hex 32
WEBHOOK_MAX_PAYLOAD_BYTES=10485760  # 10MB default, optional
